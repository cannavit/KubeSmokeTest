image: docker:latest

services:
  - docker:dind

stages:
  - smoketest

cache:
  paths:
    - .m2/repository

checkLogin:
  image: smktesting/smoke-master:latest
  stage: smoketest
  script: 
    - echo "TEST"

  # [
  #       'create-smktest',
  #       '--project-name=test',
  #       '--environment=develop',
  #       '--context=kubernetes',
  #       '--namespace=NAMESPACE',
  #       '--mode-auto=true',
  #       '--assert-curl=curl www.google.com',
  #     ]

  only:
    - master

